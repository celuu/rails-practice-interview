


<h1>Tasks</h1>

<%= link_to "New task", new_task_path %>

<ul>
  <% @tasks.each do |task| %>
    <li>
      <%= task.title %>
      <%= tag.div id: "tasks-data", data: {task: task.to_json} %>
    </li>
  <% end %>
</ul>