<h1>New donation</h1>

<%= tag.div id: "create-form", data: {users: @users.to_json, churches: @churches.to_json, donation: @donation.to_json} %>
<%= tag.div id: "show-donations", data: { donations: @donations.to_json(include: [:user, :church]), total_donation: (@total_donation || 0).to_json, by_church: (@by_church || {}).to_json } %>

<br>

<div>
  <%= link_to "Back to donations", donations_path %>
</div>
